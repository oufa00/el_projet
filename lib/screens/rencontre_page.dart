import 'package:flutter/material.dart';
class RencontrePage extends StatelessWidget {
  const RencontrePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Text("Meet Page"),
      ),
    );
  }
}
